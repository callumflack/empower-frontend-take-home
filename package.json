{
  "name": "server",
  "scripts": {
    "server": "bun run --hot server/index.ts",
    "client": "bun --cwd client dev"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
